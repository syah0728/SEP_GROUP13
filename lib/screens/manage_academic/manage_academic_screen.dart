import 'package:flutter/material.dart';

class ManageAcademicScreen extends StatelessWidget {
  const ManageAcademicScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Manage Academic screen placeholder'),
    );
  }
}
