import 'package:flutter/material.dart';

class ManageCocurriculumScreen extends StatelessWidget {
  const ManageCocurriculumScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Manage Cocurriculum screen placeholder'),
    );
  }
}
