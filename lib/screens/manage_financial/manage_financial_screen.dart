import 'package:flutter/material.dart';

class ManageFinancialScreen extends StatelessWidget {
  const ManageFinancialScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Manage Financial screen placeholder'),
    );
  }
}
